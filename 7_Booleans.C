# include <stdio.h>
// To use bool need to include bool header
# include <stdbool.h>


int main(){
    
    // boolean (bool datatype)
    // yes  or no
    // on   or off
    // True or False

    bool gamer = true;

    printf("%d\n", gamer);

    // 2 > 1 is an condition which returns either true or false
    printf("%d", 2 > 1);

    return 0;
}